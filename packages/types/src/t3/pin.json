{
  "_comment": "The pinned t3code contract. Edited only by the refresh procedure in tools/t3-codegen/REFRESH.md. Spec 146.",
  "_identities": "Spec 250: two identities, not one. `commit` keeps its spec 146 meaning (the commit the generated artifacts came from) and that source is the FORK from phase 5 onward. `upstreamBase` is the pingdotgg commit the fork branched from, and the pin the read-only upstream clone must stay on. They are equal until the fork diverges, which is deliberate: while they are equal every two-identity assertion has a known answer.",
  "repo": "https://github.com/pingdotgg/t3code",
  "commit": "082e6ea521861fff37b90fcd789b5eaa5ef5d6a6",
  "upstreamBase": "082e6ea521861fff37b90fcd789b5eaa5ef5d6a6",
  "forkRepo": "https://github.com/pseudoseed/t3code.git",
  "forkBranch": "codev",
  "contractSource": "upstream",
  "commitDate": "2026-08-25",
  "cliVersion": "0.0.36",
  "effectVersion": "4.0.0-beta.103",
  "contractsRoot": "packages/contracts/src",
  "closure": [
    "auth.ts",
    "baseSchemas.ts",
    "environment.ts",
    "git.ts",
    "model.ts",
    "orchestration.ts",
    "providerInstance.ts",
    "sourceControl.ts",
    "vcs.ts"
  ],
  "closureNote": "Transitive import closure of orchestration.ts + git.ts + auth.ts. 9 files, 3663 lines. rpc.ts is deliberately excluded: its own closure is 27 files / 11120 lines because it names every unrelated subsystem's RPCs. The generator FAILS if the real import graph reaches a file not on this list.",
  "methods": {
    "_comment": "The mapping rpc.ts would have given us, pinned explicitly instead. Orchestration entries are DERIVED from OrchestrationRpcSchemas in the closure and verified against this list. The vcs method strings live in the unvendored rpc.ts, so they are recorded here and their schemas resolved from the vendored git.ts.",
    "orchestration.dispatchCommand": {
      "key": "dispatchCommand",
      "source": "OrchestrationRpcSchemas"
    },
    "orchestration.subscribeThread": {
      "key": "subscribeThread",
      "source": "OrchestrationRpcSchemas",
      "stream": true
    },
    "orchestration.getTurnDiff": {
      "key": "getTurnDiff",
      "source": "OrchestrationRpcSchemas"
    },
    "orchestration.searchThreads": {
      "key": "searchThreads",
      "source": "OrchestrationRpcSchemas"
    },
    "vcs.createWorktree": {
      "input": "VcsCreateWorktreeInput",
      "output": "VcsCreateWorktreeResult",
      "source": "git.ts"
    },
    "vcs.removeWorktree": {
      "input": "VcsRemoveWorktreeInput",
      "output": null,
      "source": "git.ts"
    },
    "vcs.createRef": {
      "input": "VcsCreateRefInput",
      "output": "VcsCreateRefResult",
      "source": "git.ts"
    },
    "vcs.status": {
      "input": "VcsStatusInput",
      "output": "VcsStatusResult",
      "source": "git.ts"
    }
  },
  "_contractSource": "Where the VENDORED CONTRACT under packages/types/src/t3/generated was generated from, which is not the same question as where the fork checkout is. 'upstream' means the contract has not been regenerated from the fork yet, so a fork HEAD that DESCENDS from `commit` is expected and verify reports FORK_AHEAD_OF_CONTRACT at exit 0. Phase 5 regenerates and sets this to 'fork', after which being ahead is an error. A fork HEAD that does not descend from `commit` is FORK_CHECKOUT_MISMATCH and an error either way."
}
