{
  "name": "@cluesmith/codev-agent-host",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Spec 146 Phase 11: codev-agent's protocol-state surface on a port of its own, over a database snapshot. Two machines are a hard requirement, and a second Tower against the live ~/.agent-farm would share the real global.db, cron and PTY manager with the one driving actual builders.",
  "scripts": {
    "start": "tsx agent-host.ts"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.2",
    "typescript": "catalog:"
  }
}
